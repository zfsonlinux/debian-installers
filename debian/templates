Template: aboot-installer/bootdev
Type: string
Default: (hda)
Description: On which device should aboot be installed?
 Aboot needs to install the boot loader on a bootable device. This
 is usually the first hard drive, /dev/hda.
