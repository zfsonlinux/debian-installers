# the few code that really is GPL is dual-licensed anyways
zfs-fuse: possible-gpl-code-linked-with-openssl
# the script does have a stop rule, it looks like a false positive from lintian
zfs-fuse: init.d-script-possible-missing-stop
