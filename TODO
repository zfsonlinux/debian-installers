* CHECKINGSIZE does for some reason not use the item for it in the
  templates file, and instead gets an untranslatable fallback text from
  debootstrap. Fix.
* Use different waypoints on the progress bar for cdrom and network installs.
